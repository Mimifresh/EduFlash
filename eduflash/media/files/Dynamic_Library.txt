DYNAMIC LIBRARY

Things To Discuss

1) Stages of compilation of a c program
	i) Preprocessing (-E) --> Comments
	ii) compilation (-S) --> .s Assembly code
	iii) Assembly (-c) --> Object code Binary files
	iv) Linking --> Give a final executable

2) Why? and How? /* Shared, Reusable */
	-shared	 gcc -shared -fPIC func.c  -o libfunctions.so
	-fPIC
	gcc test.c -L. -lmiracle -o lab
		2 methods to create dynamic libraries
		create an object code, then link to your library
		create a library, the compile the shared library with your source code

3) Environment variable
	$LD_LIBRARY_PATH

4)	nm
	ldd
	idconfig

5) Cpython using ctypes

		